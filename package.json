{
  "name": "byshadow-portfolio",
  "version": "1.0.0",
  "scripts": {
    "install:hugo": "node scripts/install-hugo.js",
    "prebuild": "git submodule update --init --recursive",
    "build": "hugo --gc --minify --verbose"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "tar": "^6.2.1"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}