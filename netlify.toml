[build]
  command = "git submodule update --init --recursive && hugo --gc --minify"
  publish = "public"

[build.environment]
  HUGO_VERSION = "0.92.2_extended"
  PYTHON_VERSION = "3.10"  
  MISE_ENABLED = "false"